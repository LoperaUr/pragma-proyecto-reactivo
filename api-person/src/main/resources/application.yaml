spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/r_person_db
    username: root
    password: 12345
    pool:
      initial-size: 5
      max-size: 20
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
server:
  port: 8083